# Отчет об исправлениях калькулятора

## Дата: 2025-01-27

---

## ✅ ИСПРАВЛЕНИЕ 1: Нормализация единиц измерения хешрейта

### Проблема
Калькулятор сравнивал хешрейты в разных единицах без приведения к одному стандарту, что приводило к неверным расчетам доли майнера в сети.

### Что изменилось

**Файл:** `utils/calculator.py`

1. **Добавлен словарь UNIT_MULTIPLIERS** для конвертации единиц в H/s:
```python
UNIT_MULTIPLIERS = {
    "th/s": 1_000_000_000_000,  # TH/s -> H/s
    "gh/s": 1_000_000_000,      # GH/s -> H/s
    "mh/s": 1_000_000,          # MH/s -> H/s
    "kh/s": 1_000,              # KH/s -> H/s
    "h/s": 1                    # H/s -> H/s
}
```

2. **Добавлена нормализация перед расчетом share:**
```python
# Конвертируем оба значения в H/s для унификации
miner_hash_hs = miner_hash * MiningCalculator.UNIT_MULTIPLIERS.get(unit.lower(), 1)
network_hash_hs = network_hash * MiningCalculator.UNIT_MULTIPLIERS.get(network_unit.lower(), 1)

# Рассчитываем долю майнера в сети (все в H/s, поэтому корректно)
share = miner_hash_hs / network_hash_hs if network_hash_hs > 0 else 0
```

### Результат тестирования

**Тест для SHA-256 (Bitcoin):**
- Майнер: 100 TH/s = 100,000,000,000,000 H/s
- Сеть: 650,000 TH/s = 650,000,000,000,000,000 H/s
- Ожидаемая доля: 0.00015385
- Рассчитанная доля: 0.00015077 (с учетом efficiency_factor 0.98)
- ✅ **ТЕСТ ПРОЙДЕН**

**Тест для Scrypt (Litecoin):**
- Майнер: 1000 MH/s = 1,000,000,000 H/s
- Сеть: 600,000 MH/s = 600,000,000,000 H/s
- Ожидаемая доля: 0.00166667
- Рассчитанная доля: 0.00163333 (с учетом efficiency_factor 0.98)
- ✅ **ТЕСТ ПРОЙДЕН**

### Как проверить
Запустите тест:
```bash
python test_calculator_fixes.py
```

Проверьте расчеты в боте:
1. Откройте бота в Telegram
2. Выберите "Калькулятор доходности"
3. Выберите ASIC модель (например, для SHA-256)
4. Введите стоимость электроэнергии
5. Проверьте, что доходность выглядит разумной (не слишком заниженной)

---

## ✅ ИСПРАВЛЕНИЕ 2: Расчет количества монет

### Проблема
При расчете количества монет для разных криптовалют использовалась неправильная формула `daily_income_usd / coin["price"]`, что давало неверные результаты для всех монет кроме первой.

### Что изменилось

**Файл:** `utils/calculator.py`, функция `make_period`

**Было:**
```python
for symbol, coin in coin_data.items():
    coins = (daily_income_usd / coin["price"]) * multiplier  # НЕПРАВИЛЬНО!
    coins_per_coin[symbol] = coins
```

**Стало:**
```python
# Используем daily_coins напрямую для всех монет (количество монет одинаково)
for symbol in coin_data.keys():
    coins = daily_coins * multiplier
    coins_per_coin[symbol] = coins
```

### Результат тестирования

**Тест с несколькими монетами (BTC и BCH):**
- daily_coins: 0.06784615
- BTC: 0.06784615 ✅
- BCH: 0.06784615 ✅
- ✅ **ТЕСТ ПРОЙДЕН** - для всех монет одинаковое количество

### Как проверить
1. В боте выберите ASIC, который майнит несколько монет (например, SHA-256 для BTC и BCH)
2. Проверьте результат расчета
3. Убедитесь, что количество монет одинаково для всех криптовалют
4. Проверьте, что доход в USD различается только из-за разной цены монет

---

## ✅ ИСПРАВЛЕНИЕ 3: Efficiency Factor

### Проблема
`efficiency_factor` всегда был равен 1.0, что не учитывало реальные потери при майнинге (потери на пуле, задержки сети).

### Что изменилось

**Файл:** `utils/calculator.py`, функция `get_algorithm_params`

**Было:**
```python
"efficiency_factor": 1.0,  # Не учитывает потери
```

**Стало:**
```python
"efficiency_factor": 0.98,  # Учитываем потери на пуле (2%)
```

### Результат тестирования

- Efficiency factor: 0.98 ✅
- ✅ **ТЕСТ ПРОЙДЕН**

### Как проверить
1. Запустите расчет в боте
2. Доходность должна быть на 2% меньше, чем без учета потерь
3. Это более реалистичная оценка доходности

---

## 📊 Сводка изменений

| Проблема | Статус | Критичность | Тест |
|----------|--------|-------------|------|
| Нормализация единиц | ✅ Исправлено | Критично | ✅ Пройден |
| Расчет монет | ✅ Исправлено | Критично | ✅ Пройден |
| Efficiency factor | ✅ Исправлено | Важно | ✅ Пройден |

---

## 🧪 Как проверить все исправления

### Автоматическое тестирование
```bash
python test_calculator_fixes.py
```

Ожидаемый результат:
- ✅ Все 4 теста должны пройти успешно
- ✅ Нормализация единиц работает корректно
- ✅ Расчет монет правильный для всех криптовалют
- ✅ Efficiency factor установлен в 0.98

### Ручное тестирование в боте

1. **Тест для SHA-256:**
   - Выберите ASIC с SHA-256 (например, Bitmain S19)
   - Введите стоимость электроэнергии (например, 5 руб/кВт·ч)
   - Проверьте доходность - должна быть разумной (не слишком заниженной)

2. **Тест для Scrypt:**
   - Выберите ASIC с Scrypt (например, для Litecoin)
   - Проверьте расчеты

3. **Тест для нескольких монет:**
   - Выберите ASIC, который может майнить несколько монет
   - Проверьте, что количество монет одинаково для всех

---

## ⚠️ Важные замечания

1. **Данные в базе данных:** Значения `network_hashrate` в базе данных могут быть устаревшими или в неправильных единицах. Рекомендуется:
   - Проверить актуальность данных
   - Добавить поле `hashrate_unit` в таблицу `algorithm_data`
   - Регулярно обновлять значения

2. **Block time:** Значения времени блока могут требовать проверки:
   - SHA-256: 600 сек (10 мин) - правильно
   - Scrypt: 150 сек - проверить для LTC
   - Etchash: 13 сек - проверить для ETC

3. **Дальнейшие улучшения:**
   - Добавить логирование расчетов для отладки
   - Добавить валидацию входных данных
   - Добавить обработку ошибок

---

## 📝 Файлы, измененные в этом исправлении

1. `utils/calculator.py` - основные исправления
2. `test_calculator_fixes.py` - тесты для проверки (новый файл)
3. `FIXES_REPORT.md` - этот отчет (новый файл)

---

## ✅ Заключение

Все критические проблемы с калькулятором исправлены:
- ✅ Нормализация единиц измерения работает корректно
- ✅ Расчет количества монет исправлен
- ✅ Efficiency factor учитывает реальные потери

Калькулятор теперь должен давать более точные результаты, что должно решить проблемы пользователей с неточными расчетами.


